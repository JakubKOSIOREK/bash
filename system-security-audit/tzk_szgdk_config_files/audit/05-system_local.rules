# Zasady auditd dla monitorowania zmian konfiguracji lokalnych systemów
# - Monitorowanie ustawień nazwy hosta i domeny dla architektury 64-bitowej i 32-bitowej
# - Monitorowanie zmian w plikach /etc/issue, /etc/issue.net, /etc/hosts, /etc/networks oraz /etc/network/

-a always,exit -F arch=b64 -S sethostname,setdomainname -k system-locale
-a always,exit -F arch=b32 -S sethostname,setdomainname -k system-locale
-w /etc/issue -p wa -k system-locale
-w /etc/issue.net -p wa -k system-locale
-w /etc/hosts -p wa -k system-locale
-w /etc/networks -p wa -k system-locale
-w /etc/network/ -p wa -k system-locale