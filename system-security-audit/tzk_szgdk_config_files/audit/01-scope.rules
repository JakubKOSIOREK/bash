# Monitorowanie zmian zakresu dla administratorów systemu
# Ten plik zawiera zasady audytu, które obserwują zmiany w pliku /etc/sudoers
# oraz w katalogu /etc/sudoers.d. Są to kluczowe lokalizacje kontrolujące
# uprawnienia administratorów systemu.

# Obserwowanie zmian w pliku /etc/sudoers
-w /etc/sudoers -p wa -k scope

# Obserwowanie zmian w katalogu /etc/sudoers.d
-w /etc/sudoers.d -p wa -k scope
