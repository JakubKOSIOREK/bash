# Monitorowanie zdarzeń modyfikujących informacje o użytkownikach/grupach.
# Śledzi zmiany w plikach /etc/group, /etc/passwd, /etc/gshadow, /etc/shadow oraz /etc/security/opasswd.
# Opcja -w określa ścieżkę do pliku do monitorowania, -p określa uprawnienia do monitorowania (write, attribute change),
# a -k służy do oznaczenia zdarzeń kluczem 'identity', ułatwiając ich wyszukiwanie.

-w /etc/group -p wa -k identity
-w /etc/passwd -p wa -k identity
-w /etc/gshadow -p wa -k identity
-w /etc/shadow -p wa -k identity
-w /etc/security/opasswd -p wa -k identity
